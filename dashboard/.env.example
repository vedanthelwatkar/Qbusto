# Server root - NOT the API root. The backend mounts its router at /api and
# request paths already include it, so a value ending in /api produces
# /api/api/... Use the origin only.
VITE_API_URL=http://localhost:4567

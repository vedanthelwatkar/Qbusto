/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Cinema Ordering API
 * QBusto backend API. Operational probes (/health, /ready, /version) live at the root; all business endpoints live under /api.
 * OpenAPI spec version: 1.0.0
 */
import type {
  GetApiOrders200,
  GetApiOrdersId200,
  GetApiOrdersParams,
  PostApiOrders201,
  PostApiOrdersBody,
  PutApiOrdersIdPaymentStatus200,
  PutApiOrdersIdPaymentStatusBody,
  PutApiOrdersIdStatus200,
  PutApiOrdersIdStatusBody
} from '../cinemaOrderingAPI.schemas';

import { customInstance } from '../../axios-instance';



  export const getOrders = () => {
/**
 * Paginated, newest first by default. Non-owners see only orders placed at cinemas in their own chain. `status` and `paymentStatus` are filtered by status **code**, never by numeric id. `search` matches customer mobile, customer email, seat number, film title, or the order id exactly. Each row carries its cinema, screen, both statuses and its items. Requires the Orders module read permission.
 * @summary List orders
 */
const getApiOrders = (
    params?: GetApiOrdersParams,
 ) => {
      return customInstance<GetApiOrders200>(
      {url: `/api/orders`, method: 'GET',
        params
    },
      );
    }
  /**
 * The request names products and quantities; every price, discount and total is calculated by the server from `product_pricing` and any amount sent in the body is discarded. Each product must be carried at the cinema, active, inside its date range and inside one of its availability windows, and must have an active price there.
 *
 * The order, its item snapshots and the opening entry in both audit trails are written in one transaction. A new order always starts at status `initiated` with payment status `pending` - neither can be set by the request. Requires the Orders module edit permission.
 * @summary Place an order
 */
const postApiOrders = (
    postApiOrdersBody: PostApiOrdersBody,
 ) => {
      return customInstance<PostApiOrders201>(
      {url: `/api/orders`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: postApiOrdersBody
    },
      );
    }
  /**
 * Returns the order with its items, its cinema and screen, both status master rows, and both audit trails (`statusLogs`, `paymentStatusLogs`) in chronological order. Requires the Orders module read permission.
 * @summary Get one order in full
 */
const getApiOrdersId = (
    id: number,
 ) => {
      return customInstance<GetApiOrdersId200>(
      {url: `/api/orders/${id}`, method: 'GET'
    },
      );
    }
  /**
 * Transitions follow a fixed graph and arbitrary jumps are refused: `initiated` to `confirmed` to `preparing` to `ready` to `delivered`, with `rejected` reachable from any of those. `delivered` and `rejected` are terminal.
 *
 * The order update and the `order_status_logs` entry are written in one transaction. Asking for the status the order is already in is a no-op: it returns 200 and writes no log entry. Moving to `delivered` stamps `deliveredAt`. Requires the Orders module edit permission.
 * @summary Move an order to a new status
 */
const putApiOrdersIdStatus = (
    id: number,
    putApiOrdersIdStatusBody: PutApiOrdersIdStatusBody,
 ) => {
      return customInstance<PutApiOrdersIdStatus200>(
      {url: `/api/orders/${id}/status`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: putApiOrdersIdStatusBody
    },
      );
    }
  /**
 * The staff-operated payment path: cash taken at the counter, a refund granted, a failed attempt written off. Transitions are `pending` to `paid` or `failed`, `failed` back to `pending` or on to `paid`, and `paid` to `refunded`, which is terminal.
 *
 * The order update and the `payment_status_logs` entry are written in one transaction. Asking for the status the payment is already in is a no-op. This endpoint takes no gateway identifiers and does not touch the `razorpay_*` columns - gateway-driven payment confirmation belongs to the Razorpay integration phase. Requires the Orders module edit permission.
 * @summary Move an order's payment to a new status
 */
const putApiOrdersIdPaymentStatus = (
    id: number,
    putApiOrdersIdPaymentStatusBody: PutApiOrdersIdPaymentStatusBody,
 ) => {
      return customInstance<PutApiOrdersIdPaymentStatus200>(
      {url: `/api/orders/${id}/payment-status`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: putApiOrdersIdPaymentStatusBody
    },
      );
    }
  return {getApiOrders,postApiOrders,getApiOrdersId,putApiOrdersIdStatus,putApiOrdersIdPaymentStatus}};
export type GetApiOrdersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getOrders>['getApiOrders']>>>
export type PostApiOrdersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getOrders>['postApiOrders']>>>
export type GetApiOrdersIdResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getOrders>['getApiOrdersId']>>>
export type PutApiOrdersIdStatusResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getOrders>['putApiOrdersIdStatus']>>>
export type PutApiOrdersIdPaymentStatusResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getOrders>['putApiOrdersIdPaymentStatus']>>>

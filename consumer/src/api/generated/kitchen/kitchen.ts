/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Cinema Ordering API
 * QBusto backend API. Operational probes (/health, /ready, /version) live at the root; all business endpoints live under /api.
 * OpenAPI spec version: 1.0.0
 */
import type {
  GetApiKitchenOrders200,
  GetApiKitchenOrdersId200,
  GetApiKitchenOrdersParams,
  PatchApiKitchenOrdersIdStatus200,
  PatchApiKitchenOrdersIdStatusBody
} from '../cinemaOrderingAPI.schemas';

import { customInstance } from '../../axios-instance';



  export const getKitchen = () => {
/**
 * The KDS board. Returns only orders that are **paid** and in a fulfilment status the kitchen owns - this is enforced server-side and there is no parameter that relaxes it, so an unpaid or rejected order can never appear on a kitchen screen.
 *
 * `scope=active` (the default) returns work still outstanding: `confirmed`, `preparing`, `ready`. `scope=completed` returns `delivered`. `scope=all` returns both in one request.
 *
 * Sorted oldest-first by default, because a kitchen works a queue. `search` matches seat number, film title, or the order id exactly. Non-owners see only orders placed at cinemas in their own chain. Requires the Orders module read permission.
 * @summary List orders the kitchen is responsible for
 */
const getApiKitchenOrders = (
    params?: GetApiKitchenOrdersParams,
 ) => {
      return customInstance<GetApiKitchenOrders200>(
      {url: `/api/kitchen/orders`, method: 'GET',
        params
    },
      );
    }
  /**
 * The focused ticket view. The same eligibility rule as the board is applied here, so an order that exists but is unpaid, rejected, or in another chain is reported as **404** rather than 403 - the kitchen has no business learning that such an order exists. Requires the Orders module read permission.
 * @summary Get one kitchen order in full
 */
const getApiKitchenOrdersId = (
    id: number,
 ) => {
      return customInstance<GetApiKitchenOrdersId200>(
      {url: `/api/kitchen/orders/${id}`, method: 'GET'
    },
      );
    }
  /**
 * Applies one forward step of the fulfilment graph: `confirmed -> preparing -> ready -> delivered`. The move is validated against the order's **current** status on the server, so a stale screen cannot skip a step.
 *
 * The kitchen may only set `preparing`, `ready` or `delivered`. `rejected` is a commercial decision that belongs to the Dashboard, and `confirmed` is set by payment rather than by a person.
 *
 * Requesting the status the order is already in is a no-op and returns **200** with no new audit entry - two screens pressing the same button is a race, not an error. A move that lost such a race, or one that is illegal from the current status, returns **409** with the statuses the order could legally reach; the client should replace its copy with the server's. Requires the Orders module edit permission.
 * @summary Move an order along the kitchen workflow
 */
const patchApiKitchenOrdersIdStatus = (
    id: number,
    patchApiKitchenOrdersIdStatusBody: PatchApiKitchenOrdersIdStatusBody,
 ) => {
      return customInstance<PatchApiKitchenOrdersIdStatus200>(
      {url: `/api/kitchen/orders/${id}/status`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: patchApiKitchenOrdersIdStatusBody
    },
      );
    }
  return {getApiKitchenOrders,getApiKitchenOrdersId,patchApiKitchenOrdersIdStatus}};
export type GetApiKitchenOrdersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getKitchen>['getApiKitchenOrders']>>>
export type GetApiKitchenOrdersIdResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getKitchen>['getApiKitchenOrdersId']>>>
export type PatchApiKitchenOrdersIdStatusResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getKitchen>['patchApiKitchenOrdersIdStatus']>>>
